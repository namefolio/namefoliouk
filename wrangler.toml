# Cloudflare Pages: serve the Vite build output, not the repo root.
# The build command itself (npm run build) is set in the Pages dashboard.
name = "namefoliouk"
compatibility_date = "2026-09-01"
pages_build_output_dir = "dist"
